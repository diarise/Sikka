# Copy this file to ".env" (same folder as sync_agent.py) and fill in real values.
# Never commit the real ".env" file to git or ship it inside a compiled .exe's
# visible resources — it should be dropped onto each merchant machine at install
# time, ideally with restricted file permissions.

# Use the anon/public key here if you've moved writes to an RPC/Edge Function
# as described in the SECURITY NOTE at the bottom of sync_agent.py.
# If you must use service_role short-term, rotate it immediately after v3.4's
# key is retired, and treat this .env file as sensitive as a password vault.
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_KEY=your-key-here

# How often (seconds) the agent pulls from Sage 100 and pushes to Supabase.
SIKKA_SYNC_INTERVAL=60
